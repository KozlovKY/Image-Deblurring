# Image-Deblurring
The project solves the problem of restoring sharpness to blurred images using neural networks. The model is trained on pairs of blurred-sharp images. As is known, Gaussian filter transformation is irreversible, so neural network-based deblurring is a standard tool in this domain
